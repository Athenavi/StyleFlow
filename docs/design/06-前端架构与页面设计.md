# 06 - 前端架构与页面设计

> **版本**：v1.0 | **更新**：2026-06-26

---

## 6.1 技术栈

| 技术 | 用途 |
|------|------|
| **Next.js 14 + App Router** | 框架，SSR/SSG/CSR 灵活选择 |
| **Ant Design 5.x** | UI 组件库，中后台场景 |
| **Axios** | HTTP 请求封装 |
| **zustand** | 轻量级状态管理（可选，大部分场景用 URL 状态 + Server State） |
| **@ant-design/icons** | 图标 |
| **dayjs** | 日期处理 |
| **tailwindcss** | 辅助样式（Ant Design 覆盖不了的部分） |

---

## 6.2 页面路由设计

```
/                               # 重定向到 /dashboard 或 /login
├── /login                      # 登录页
├── /register                   # 注册页
│
├── /dashboard                  # 工作台（首页）
│   ├── 概览卡片：我的设计数、待办任务数、近期待办
│   ├── 最新设计稿（最近 6 张缩略图）
│   └── 快捷入口：新建设计、虚拟试衣、查看流程
│
├── /design                     # AI 设计工坊
│   ├── /generate               # 文生图 / 图生图
│   │   ├── 左侧：Prompt 输入区（模板选择、参数调节）
│   │   ├── 右侧：结果展示区（图片卡片、下载、收藏）
│   │   └── 底部：历史生成记录
│   └── /gallery                # 设计稿库
│       ├── 卡片网格展示
│       ├── 筛选栏（分类、标签、时间）
│       ├── 搜索框
│       └── 点击进入详情（版本历史、关联工艺单）
│
├── /tryon                      # 虚拟试衣间
│   ├── 左侧：模特展示区（可上传/选用默认模特）
│   ├── 右侧：单品库（设计稿列表，可筛选分类）
│   ├── 底部：合成按钮 + 历史记录
│   └── 拖拽交互：将单品拖到模特上触发合成
│
├── /techpack                   # 工艺单管理
│   ├── 列表页（表格视图，筛选、搜索）
│   └── /[id]                   # 详情/编辑页
│       ├── 款式信息：款号、品类、面料
│       ├── 尺码表：可编辑表格
│       ├── 工序列表：从模板选择/手动添加
│       └── 操作：提交审核、导出 PDF
│
├── /workflow                   # 工作流中心
│   ├── 看板视图（待办/进行中/已完成）
│   ├── /[id]                   # 流程详情
│   │   ├── 流程图：节点 + 连线 + 状态
│   │   ├── 操作按钮：推进、驳回
│   │   └── 操作历史时间线
│   └── /config                 # 流程配置（管理员）
│
├── /erp                        # ERP 数据查询
│   ├── 款式列表（搜索款号、品类筛选）
│   ├── /[code]                 # 款式详情（BOM、工序）
│   └── 数据同步状态面板
│
├── /costing                    # 核工价
│   ├── 待核价列表
│   └── /[id]                   # 核价详情（工序明细、总价、审批操作）
│
├── /wages                      # 计件工资
│   ├── 报表概览（月度汇总、图表）
│   └── 明细查询（按工人、日期筛选）
│
└── /admin                      # 系统管理
    ├── /users                  # 用户管理（管理员）
    └── /settings               # 系统配置（AI 参数、同步设置）
```

---

## 6.3 核心页面原型

### 6.3.1 AI 设计生成页 (`/design/generate`)

```
┌─────────────────────────────────────────────────────┐
│  Header  [StyleFlow]  [设计工坊 ▼] [虚拟试衣] [...] │
├──────────┬──────────────────────────────────────────┤
│  Prompt  │                                          │
│ 输入区    │           生成结果展示区                  │
│          │                                          │
│ ┌──────┐ │  ┌──────┐  ┌──────┐  ┌──────┐          │
│ │提示词 │ │  │ img1 │  │ img2 │  │ img3 │          │
│ │输入框 │ │  └──────┘  └──────┘  └──────┘          │
│ │       │ │                                          │
│ │[选择  │ │  操作按钮：收藏 / 下载 / 再次生成        │
│ │ 模板] │ │                                          │
│ │       │ │  ┌─ 参数面板（可折叠）────────────────┐ │
│ │[高级  │ │  │ 宽:512  高:768  步数:25  CFG:7    │ │
│ │ 参数] │ │  │ 种子: -1 (随机)                    │ │
│ │       │ │  └────────────────────────────────────┘ │
│ │       │ │                                          │
│ │[生成] │ │  ┌─ 历史记录 ──────────────────────────┐ │
│ └──────┘ │  │  · 连衣裙 夏季新款    2026-06-26     │ │
│          │  │  · 西装外套 商务系列   2026-06-25     │ │
│          │  └──────────────────────────────────────┘ │
├──────────┴──────────────────────────────────────────┤
│  Footer                                              │
└─────────────────────────────────────────────────────┘
```

### 6.3.2 虚拟试衣间 (`/tryon`)

```
┌─────────────────────────────────────────────────────┐
│  Header  [StyleFlow]  [设计工坊]  [虚拟试衣 ▼] [...]│
├───────────────────────┬─────────────────────────────┤
│                       │                             │
│   模特展示区           │    单品库                   │
│                       │                             │
│  ┌─────────────────┐  │  ┌────┐ ┌────┐ ┌────┐     │
│  │                 │  │  │上装│ │下装│ │连衣裙    │
│  │   人物照片       │  │  └────┘ └────┘ └────┘     │
│  │   (可拖拽替换)   │  │                             │
│  │                 │  │  ┌────┐ ┌────┐ ┌────┐     │
│  │   合成结果       │  │  │    │ │    │ │    │     │
│  │   (等待生成)     │  │  └────┘ └────┘ └────┘     │
│  │                 │  │                             │
│  └─────────────────┘  │  [上装 ▼] [搜索单品...]    │
│                       │                             │
│  [上传模特] [保存]    │  点击单品 → 自动开始合成    │
│                       │                             │
└───────────────────────┴─────────────────────────────┘
```

### 6.3.3 工作流看板 (`/workflow`)

```
┌─────────────────────────────────────────────────────┐
│  Header  [StyleFlow]  [工作流中心]                   │
├─────────────────────────────────────────────────────┤
│  [待办]        [进行中]        [已完成]              │
│                                                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ SS24001  │  │ SS24005  │  │ SS23998  │          │
│  │ 夏季连衣裙│  │ 西装外套 │  │ 衬衫改版 │          │
│  │ 等待审核  │  │ 工艺单   │  │ 已完成   │          │
│  │ [处理]   │  │ [查看]   │  │ [查看]   │          │
│  ├──────────┤  ├──────────┤  ├──────────┤          │
│  │ SS24002  │  │ SS24006  │  │ ...      │          │
│  │ 针织衫   │  │ 核工价   │  │          │          │
│  │ 等待核价  │  │ 待审批   │  │          │          │
│  └──────────┘  └──────────┘  └──────────┘          │
└─────────────────────────────────────────────────────┘
```

---

## 6.4 状态管理策略

| 数据类型 | 管理方式 | 说明 |
|---------|---------|------|
| **用户认证** | `zustand` store + localStorage | token 存储、用户信息缓存 |
| **API 数据** | React Query (`@tanstack/react-query`) | 列表/详情自动缓存、重新验证 |
| **UI 状态** | URL search params | 筛选、分页、搜索词等 → 可分享 URL |
| **全局 UI** | React Context | 主题、侧边栏折叠、通知 |
| **表单状态** | Ant Design Form | 自带状态管理 |

---

## 6.5 API 调用封装

```typescript
// lib/api.ts
import axios from 'axios';
import { message } from 'antd';

const api = axios.create({
  baseURL: process.env.NEXT_PUBLIC_API_URL || '/api/v1',
  timeout: 30000,
});

// 请求拦截器：注入 JWT
api.interceptors.request.use((config) => {
  const token = localStorage.getItem('access_token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// 响应拦截器：统一错误处理
api.interceptors.response.use(
  (response) => response.data,
  (error) => {
    if (error.response?.status === 401) {
      // Token 过期，尝试刷新
      // 刷新失败则跳转登录
      window.location.href = '/login';
    }
    const msg = error.response?.data?.error?.message || '请求失败';
    message.error(msg);
    return Promise.reject(error);
  }
);

export default api;
```

### 类型定义示例

```typescript
// lib/types/design.ts
export interface Design {
  id: number;
  title: string;
  image_url: string;
  thumbnail_url: string;
  prompt: string;
  category: 'sketch' | 'style' | 'fabric' | 'moodboard' | 'flatten';
  tags: string[];
  status: 'draft' | 'completed' | 'archived';
  created_at: string;
}

export interface PaginatedResponse<T> {
  success: boolean;
  data: T[];
  pagination: {
    total: number;
    page: number;
    page_size: number;
    total_pages: number;
  };
}
```

---

## 6.6 响应式与移动端适配

| 断点 | 宽度 | 布局 |
|------|------|------|
| xs | < 576px | 单列，侧边栏隐藏为汉堡菜单 |
| sm | ≥ 576px | 单列，部分组件显示 |
| md | ≥ 768px | 两栏布局 |
| lg | ≥ 992px | 三栏布局（设计生成页） |
| xl | ≥ 1200px | 完整桌面布局 |

- **目标**：核心页面（设计生成、虚拟试衣）在 iPad / 平板端可流畅使用
- **小程序**：独立 UI，代码复用 API 调用层和类型定义
